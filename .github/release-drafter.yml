---
include-pre-releases: true
name-template: 'v$RESOLVED_VERSION'
tag-template: '$RESOLVED_VERSION'
replacers:
  - search: '/(chore\(app\)\: Update )(todo)(.+)/g'
    replace: '$1[$2](https://github.com/matthiasbalke/todo)$3'
template: |
  # What's Changed

  $CHANGES

  ## New Contributors

  $NEW_CONTRIBUTORS

  **Full Changelog**: https://github.com/$OWNER/$REPOSITORY/compare/$PREVIOUS_TAG...v$RESOLVED_VERSION

categories:
  - title: '📅 Todo'
    exclusive: true
    when:
      conventional:
        - types:
            - 'chore'
        - scopes:
            - 'app'
  - title: '🚨 Breaking Changes'
    semver-increment: 'major'
    exclusive: true
    when:
      conventional:
        breaking: true
  - title: '✨ Features'
    semver-increment: 'minor'
    exclusive: true
    when:
      conventional:
        type: 'feat'
  - title: '⬆️ Dependency Updates'
    semver-increment: 'patch'
    exclusive: true
    when:
      conventional:
        scopes:
          - 'deps'
          - 'deps-dev'
        types:
          - 'build'
          - 'chore'
          - 'ci'
          - 'fix'
    collapse-after: 5
  - title: '🐛 Bug Fixes'
    semver-increment: 'patch'
    exclusive: true
    when:
      conventional:
        type: 'fix'
  - title: '🧰 Maintenance'
    semver-increment: 'patch'
    exclusive: true
    when:
      conventional:
        types:
          - 'build'
          - 'chore'
          - 'ci'
          - 'perf'
          - 'refactor'
          - 'revert'
          - 'style'
          - 'test'
  - title: '📚 Documentation'
    semver-increment: 'patch'
    exclusive: true
    when:
      conventional:
        type: 'docs'
  - title: 'Other changes'
  - type: 'pre-exclude'
    when:
      label: 'skip-changelog'
